{
    "main": "app/bot.js",
    "type": "module",
    "scripts": {
        "build": "esbuild 'src/'*'.ts' --outdir='app' --bundle --platform=node --format=esm --external:'./node_modules/*' --analyze",
        "start": "node --expose-gc ."
    },
    "dependencies": {
        "discord-slim": "2.7",
        "fast-xml-parser": "4.0"
    },
    "devDependencies": {
        "@types/node": "*",
        "esbuild": "0.14"
    }
}
